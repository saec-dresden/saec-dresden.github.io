"use strict";var grunt=require("grunt");var comment=require("../tasks/lib/comment").init(grunt);exports.concat={default_options:function(c){c.expect(1);var a=grunt.file.read("tmp/default_options");var b=grunt.file.read("test/expected/default_options");c.equal(a,b,"should describe what the default behavior is.");c.done()},custom_options:function(c){c.expect(1);var a=grunt.file.read("tmp/custom_options");var b=grunt.file.read("test/expected/custom_options");c.equal(a,b,"should utilize custom banner, footer and separator.");c.done()},handling_invalid_files:function(c){c.expect(1);var a=grunt.file.read("tmp/handling_invalid_files");var b=grunt.file.read("test/expected/handling_invalid_files");c.equal(a,b,"will have warned, but should not fail.");c.done()},strip_banner:function(b){b.expect(7);var a=grunt.file.read("test/fixtures/banner.js");b.equal(comment.stripBanner(a),"// Comment\n\n/* Comment */\n","It should strip the top banner.");b.equal(comment.stripBanner(a,{block:true}),"// Comment\n\n/* Comment */\n","It should strip the top banner.");a=grunt.file.read("test/fixtures/banner2.js");b.equal(comment.stripBanner(a),"\n/*! SAMPLE\n * BANNER */\n\n// Comment\n\n/* Comment */\n","It should not strip the top banner.");b.equal(comment.stripBanner(a,{block:true}),"// Comment\n\n/* Comment */\n","It should strip the top banner.");a=grunt.file.read("test/fixtures/banner3.js");b.equal(comment.stripBanner(a),"\n// This is\n// A sample\n// Banner\n\n// But this is not\n\n/* And neither\n * is this\n */\n","It should not strip the top banner.");b.equal(comment.stripBanner(a,{block:true}),"\n// This is\n// A sample\n// Banner\n\n// But this is not\n\n/* And neither\n * is this\n */\n","It should not strip the top banner.");b.equal(comment.stripBanner(a,{line:true}),"// But this is not\n\n/* And neither\n * is this\n */\n","It should strip the top banner.");b.done()}};